The destructuring assignment [nums[j], nums[j+1]] = [nums[j+1], nums[j]] is a concise way to swap elements without needing an explicit temporary variable, but the operation still requires O(1) extra space.




